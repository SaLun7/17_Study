# 04. 타이타닉 생존 예측

타이타닉 승객 데이터의 결측치와 범주형 변수를 처리하고 여러 분류 모델로 생존 여부를 예측한다.

[← 과정 목차](<../README.md>) · [원본 노트북](<04_타이타닉 생존 예측.ipynb>)

## 실습 내용

- 결측치와 불필요한 열을 처리한다.
- 범주형 열을 일관되게 인코딩한다.
- 결정트리·랜덤포레스트·로지스틱 회귀를 같은 조건에서 비교한다.
- 예측 결과를 제출 형식으로 구성한다.

**데이터**  
Titanic train/test CSV: Survived가 목표이며 Age, Sex, Pclass, Fare, Embarked 등을 입력으로 사용

## 핵심 메모

### 결측치 처리

Age는 대표값, Cabin과 Embarked는 별도 범주 등 열의 의미에 맞는 전략이 필요하다.

### 데이터 누수

검증·테스트 정보를 이용해 대체값이나 범주 사전을 만들면 실제 성능보다 높게 보일 수 있다.

### 모델 비교

전처리와 데이터 분할을 고정해야 모델 점수 차이가 의미를 가진다.

## 코드

코드는 길어서 중요한 셀만 접어 두었다. 전체 실행 과정은 원본 노트북에서 확인한다.

<details>
<summary>코드 셀 8 보기</summary>

```python
test_df["Age"] = test_df["Age"].fillna(test_df["Age"].mean())
test_df["Cabin"] = test_df["Cabin"].fillna("N")
test_df["Embarked"] = test_df["Embarked"].fillna("N")
test_df["Fare"] = test_df["Fare"].fillna(0)
test_df["Cabin"] = test_df["Cabin"].str[:1]

test_df.head()
```

</details>

<details>
<summary>코드 셀 10 보기</summary>

```python
from sklearn.preprocessing import LabelBinarizer, OneHotEncoder, OrdinalEncoder
```

</details>

<details>
<summary>코드 셀 13 보기</summary>

```python
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import RandomForestClassifier
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score

dt_clf = DecisionTreeClassifier(random_state=42)
rf_clf = RandomForestClassifier(random_state=42)
lr_clf = LogisticRegression(solver="liblinear")

dt_clf.fit(X_train, y_train)
df_pred = dt_clf.predict(X_valid)
print(accuracy_score(y_valid, df_pred))

rf_clf.fit(X_train, y_train)
rf_pred = rf_clf.predict(X_valid)
print(accuracy_score(y_valid, rf_pred))

lr_clf.fit(X_train, y_train)
lr_pred = lr_clf.predict(X_valid)
print(accuracy_score(y_valid, lr_pred))
```

</details>

<details>
<summary>코드 셀 14 보기</summary>

```python
dt_test = dt_clf.predict(test_df)

rf_test = rf_clf.predict(test_df)

lr_test = lr_clf.predict(test_df)

result = pd.DataFrame({"PassengerId" : res,
                       "DecisionTree" : dt_test,
                       "RandomForest" : rf_test,
                       "LogisticRegression" : lr_test
                       })
```

</details>

## 결과 메모

세 모델의 예측 분포가 다르면 어떤 승객에서 판단이 갈리는지 확인할 수 있다. 단순 생존자 수보다 검증 정확도와 클래스별 오류를 우선 해석한다.

## 사용한 함수

| 함수·클래스 | 역할 |
|---|---|
| `read_csv` | CSV 파일을 DataFrame으로 읽기 |
| `train_test_split` | 학습용과 평가용 데이터 분리 |
| `OneHotEncoder` | 범주별 0/1 열 생성 |
| `OrdinalEncoder` | 순서형 범주를 정수로 변환 |
| `Binarizer` | 지정 임계값으로 연속값을 0/1 변환 |
| `DecisionTreeClassifier` | 결정트리 분류 모델 |
| `LogisticRegression` | 확률 기반 선형 분류 모델 |
| `RandomForestClassifier` | 여러 무작위 트리를 결합한 분류 모델 |
| `accuracy_score` | 분류 정확도 계산 |

## 정리

- ID와 이름 같은 열의 예측 가치와 누수 가능성을 확인한다.
- train과 test에 같은 전처리 객체를 사용한다.
- 성별·객실등급별 성능 차이도 함께 점검한다.
