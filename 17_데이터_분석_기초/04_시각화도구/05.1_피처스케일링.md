# 05-1. 날짜·시간 변환

[← 폴더 목차](<README.md>) · [원본 노트북](<05.1_피처스케일링.ipynb>)

## 메모

- 문자열을 Timestamp로 변경
- Timestamp를 Period로 변환
- 시계열 데이터 만들기
- 날짜데이터 분리
- 날짜데이터 분리2
- 시간 인덱스 활용
- 시계열 변환

## 사용한 문법·함수

`pd.read_csv` · `print` · `df.head` · `df.info` · `pd.to_datetime` · `type` · `pd.DatetimeIndex` · `pd.Timestamp` · `ts_dates.to_period` · `pd.date_range` · `dt.to_period` · `str.fullmatch`

## 사용한 데이터

- `stock-data.csv`
