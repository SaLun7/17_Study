# 06. 시계열데이터

[← 폴더 목차](<README.md>) · [원본 노트북](<06_시계열데이터.ipynb>)

## 메모

- 날짜 문자열을 datetime으로 변환
- 날짜 열을 인덱스로 지정하고 시간순으로 정렬
- 주가 데이터를 시계열 그래프로 확인

## 사용한 문법·함수

`pd.read_csv` · `pd.to_datetime` · `df.drop` · `sort_index` · `df.head` · `df.set_index` · `plot` · `plt.show` · `df.plot` · `plt.subplots` · `set_title` · `def get_financials`

## 사용한 데이터

- `stock-data.csv`
