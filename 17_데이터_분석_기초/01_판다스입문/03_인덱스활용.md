# 03. 인덱스 활용

[← 폴더 목차](<README.md>) · [원본 노트북](<03_인덱스활용.ipynb>)

## 메모

- 행 인덱스 재배열
- 데이터프레임객체.reindex(인덱스배열)
- 행 인덱스 초기화
- 행 인덱스를 기준으로 데이터프레임 정렬
- 행 인덱스 기준 정렬 : DataFrame객체.sort_index()

## 사용한 문법·함수

`pd.DataFrame` · `print` · `df.set_index` · `ndf.set_index` · `df.reindex` · `df.reset_index` · `df.sort_index` · `df.sort_values`
