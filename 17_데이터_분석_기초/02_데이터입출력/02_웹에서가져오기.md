# 02. 웹에서 가져오기

[← 폴더 목차](<README.md>) · [원본 노트북](<02_웹에서가져오기.ipynb>)

## 메모

- 웹 페이지의 표를 `read_html`로 바로 읽기
- requests와 BeautifulSoup로 필요한 영역 선택
- 정규표현식으로 가져온 문자열 정리

## 사용한 문법·함수

`pd.read_html` · `print` · `len` · `range` · `type` · `requests.get` · `BeautifulSoup` · `soup.select` · `re.findall`

## 사용한 데이터

- `sample.html`
