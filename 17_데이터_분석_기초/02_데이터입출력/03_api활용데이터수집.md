# 03. API 데이터 수집

[← 폴더 목차](<README.md>) · [원본 노트북](<03_api활용데이터수집.ipynb>)

## 메모

- 주소 목록 JSON 불러오기
- geocoding API로 주소의 위도·경도 요청
- 받은 좌표를 DataFrame 열로 추가

## 사용한 문법·함수

`def get_geocoding` · `pd.DataFrame` · `print` · `requests.get` · `response.json` · `get_geocoding` · `lat.append` · `lng.append`

## 사용한 데이터

- `address.json`
