# 01. 함수매핑

[← 폴더 목차](<README.md>) · [원본 노트북](<01_함수매핑.ipynb>)

## 메모

- 개별 원소에 함수 매핑
- titanic["sex"].value_counts()
- 데이터프레임 원소에 함수 매핑
- 데이터프레임객체.apply(매핑함수, axis=0)
- 데이터프레임객체.apply(매핑함수, axis=1)
- 조건식을 만족하는 열을 필터링
- 함수 매핑 응용 - 조건부 열 추가
- 데이터프레임 객체에 함수 매핑

## 사용한 문법·함수

`sns.load_dataset` · `titanic.head` · `df.head` · `def add_10` · `def add_two_obj` · `print` · `add_10` · `add_two_obj` · `head` · `apply` · `sr1.head` · `sr2.head`

## 사용한 데이터

- `seaborn: titanic`
