# 04. 멀티인덱스

[← 폴더 목차](<README.md>) · [원본 노트북](<04_멀티인덱스.ipynb>)

## 메모

- 데이터 추출(멀티 인덱싱)
- 데이터 정렬

## 사용한 문법·함수

`sns.load_dataset` · `df.head` · `df.groupby` · `grouped.agg` · `pd.MultiIndex.from_arrays` · `pd.DataFrame` · `pd.MultiIndex.from_tuples` · `pd.MultiIndex.from_frame` · `multi_index_frame.get_level_values` · `gdf.columns.get_level_values` · `type` · `gdf.xs`

## 사용한 데이터

- `seaborn: titanic`
