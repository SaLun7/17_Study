# 05. 데이터프레임합치기

[← 폴더 목차](<README.md>) · [원본 노트북](<05_데이터프레임합치기.ipynb>)

## 메모

- 데이터프레임 연결
- 데이터프레임 병합
- 데이터프레임조회
- 행 인덱스 기준으로 결합 : DataFrame1.joun(DataFrame2, how="left")

## 사용한 문법·함수

`pd.DataFrame` · `print` · `pd.concat` · `pd.Series` · `sr4.to_frame` · `pd.read_excel` · `pd.merge` · `def get_financials` · `requests.get` · `response.json` · `get_financials` · `df_revenue.set_index`

## 사용한 데이터

- `stock_price.xlsx`
- `stock_valuation.xlsx`
