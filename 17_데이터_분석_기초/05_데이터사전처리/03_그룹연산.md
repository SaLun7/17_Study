# 03. 그룹연산

[← 폴더 목차](<README.md>) · [원본 노트북](<03_그룹연산.ipynb>)

## 메모

- 그룹 객체만들기
- 개별그룹 선택하기
- 여러열을 기준으로 그룹화
- 변환  Transformation
- 그룹 객체필터링 : group객체.filter(조건식함수)
- 그룹 객체에 함수 매핑

## 사용한 문법·함수

`sns.load_dataset` · `df.head` · `value_counts` · `nunique` · `unique` · `df.groupby` · `print` · `len` · `group.head` · `grouped.mean` · `grouped.get_group` · `grouped_two.mean`

## 사용한 데이터

- `seaborn: titanic`
