# 25. 딕셔너리응용

[← 폴더 목차](<README.md>) · [원본 노트북](<25_딕셔너리응용.ipynb>)

## 메모

- 딕셔너리 조작하기
- setdefault, update
- 딕셔너리의 키의 값 수정
- 딕셔너리에 키-값 쌍 삭제하기
- 딕셔너리에 키의 값을 가져오기
- 딕셔너리에서 키-값을 모두 가져오기
- 리스트와 튜플로 딕셔너리 만들기
- 반복문으로 딕셔너리의 카-값 쌍을 모두 출력하기
- 딕셔너리 표현식 사용하기
- {키:값 for 키, 값 in 딕셔너리}

## 사용한 문법·함수

`a.setdefault` · `print` · `a.update` · `y.update` · `zip` · `x.pop` · `x.popitem` · `x.clear` · `x.get` · `x.items` · `list` · `x.keys`
