[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "colab-local-shim"
version = "0.1.0"
description = "Minimal local replacements for commonly used google.colab APIs"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

