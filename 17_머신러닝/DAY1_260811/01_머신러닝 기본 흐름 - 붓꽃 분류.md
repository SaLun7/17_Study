# 01. 머신러닝 기본 흐름 - 붓꽃 분류

Iris 데이터를 이용해 입력과 정답을 준비하고, 결정트리를 학습한 뒤 새로운 붓꽃의 품종을 예측한다.

[← DAY1 목차](<README.md>) · [원본 노트북](<01_머신러닝 기본 흐름 - 붓꽃 분류.ipynb>)

## 실습 내용

- 머신러닝의 기본 진행 순서를 이해한다.
- 피처 X와 레이블 y를 구분한다.
- 학습용과 테스트용 데이터를 분리한다.
- 결정트리의 예측을 정확도로 평가한다.

**데이터**  
Iris 데이터셋: 꽃받침과 꽃잎의 길이·너비 4개 피처, setosa·versicolor·virginica 3개 클래스

## 핵심 메모

### 피처와 레이블

피처는 모델이 사용하는 입력 정보이고 레이블은 모델이 맞혀야 하는 정답이다.

### 학습과 테스트

모델이 보지 못한 데이터로 평가해야 실제 일반화 성능을 추정할 수 있다.

### 결정트리

입력값에 조건을 반복 적용해 공간을 나누고 최종 클래스를 결정한다.

## 코드

코드는 길어서 중요한 셀만 접어 두었다. 전체 실행 과정은 원본 노트북에서 확인한다.

<details>
<summary>코드 셀 1 보기</summary>

```python
# 머신러닝에 필요한 여러 기능을 제공하는 scikit-learn 라이브러리를 불러온다
import sklearn
# sklearn의 예제 데이터인 iris 데이터를 불러오는 load_iris 함수를 가져온다
from sklearn.datasets import load_iris
# 결정 트리를 이용해 분류 모델을 학습하고 예측하기 위해 가져온다
from sklearn.tree import DecisionTreeClassifier
# 데이터를 모두 학습용과 성능 평과용으로 나누기 위해 가져온다
from sklearn.model_selection import train_test_split
# 표 형태의 데이터를 다루고 분석하기 위해 pandas를 pd라는 이름으로 불러온다
import pandas as pd
```

</details>

<details>
<summary>코드 셀 5 보기</summary>

```python
# 결정 트리 분류 모델을 생성하고, 같은 조건에서 결과를 재현할 수 있도록 random_state를 42로 고정한다
dt_clf = DecisionTreeClassifier(random_state=42)

# 학습용 문제 X_train과 정답 y_train을 이용해 결정 트리 모델을 학습시킨다
dt_clf.fit(X_train, y_train);
```

</details>

<details>
<summary>코드 셀 6 보기</summary>

```python
# 테스트용 문제 X_test를 학습된 결정 트리 모델에 넣어 예측하고, 그 예측값을 pred에 저장한다
pred = dt_clf.predict(X_test)

# 예측 값을 출력한다
print(pred)
```

</details>

<details>
<summary>코드 셀 7 보기</summary>

```python
# 실제 정답과 예측값을 비교해 정확도를 계산하기 위해 accuracy_score를 불러온다
from sklearn.metrics import accuracy_score

# 실제 정답 y_test와 모델의 예측값 pred를 비교해 정확도를 출력한다
print(accuracy_score(y_test, pred))
```

</details>

## 결과 메모

예측값과 정확도는 모델 학습이 정상적으로 수행됐는지 확인하는 기준이다. 정확도는 클래스별 오류를 보여주지 않으므로 다음 단계에서는 혼동행렬과 함께 보는 것이 좋다.

## 사용한 함수

| 함수·클래스 | 역할 |
|---|---|
| `load_iris` | Iris 데이터셋 불러오기 |
| `train_test_split` | 학습용과 평가용 데이터 분리 |
| `DecisionTreeClassifier` | 결정트리 분류 모델 |
| `accuracy_score` | 분류 정확도 계산 |

## 정리

- 데이터 준비 → 분리 → 학습 → 예측 → 평가가 기본 흐름이다.
- random_state는 실험을 재현할 수 있게 한다.
- 테스트 데이터는 학습과 전처리 기준 결정에 사용하지 않는다.
