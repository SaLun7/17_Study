# 08. 앙상블 학습

Voting, Random Forest, Gradient Boosting을 이용해 여러 모델을 결합하는 방식을 비교한다.

[← DAY3 목차](<README.md>) · [원본 노트북](<08_앙상블 학습.ipynb>)

## 실습 내용

- Hard/Soft Voting의 차이를 이해한다.
- Bagging과 Boosting의 학습 방식을 구분한다.
- 랜덤포레스트의 무작위성과 분산 감소를 설명한다.
- 앙상블 모델의 특성 중요도를 해석한다.

**데이터**  
유방암 및 Human Activity Recognition 분류 데이터

## 핵심 메모

### Voting

서로 다른 모델의 클래스 또는 확률을 결합한다.

### Random Forest

여러 부트스트랩 표본과 무작위 피처로 독립 트리를 학습해 평균한다.

### Gradient Boosting

이전 모델의 오류를 다음 모델이 순차적으로 보정한다.

## 코드

코드는 길어서 중요한 셀만 접어 두었다. 전체 실행 과정은 원본 노트북에서 확인한다.

<details>
<summary>코드 셀 2 보기</summary>

```python
lr_clf = LogisticRegression(solver='liblinear')
knn_clf = KNeighborsClassifier(n_neighbors=8)

vo_clf = VotingClassifier( estimators=[('LR',lr_clf),('KNN',knn_clf)] , voting='soft' )

X_train, X_test, y_train, y_test = train_test_split(cancer.data, cancer.target, 
                                                    test_size=0.2 , random_state= 42)

vo_clf.fit(X_train , y_train)
pred = vo_clf.predict(X_test)
print(accuracy_score(y_test , pred))
```

</details>

<details>
<summary>코드 셀 6 보기</summary>

```python
from sklearn.ensemble import RandomForestClassifier
from sklearn.metrics import accuracy_score
import pandas as pd

X_train, X_test, y_train, y_test = get_human_dataset()

rf_clf = RandomForestClassifier(random_state=42)
rf_clf.fit(X_train , y_train.astype(int))
pred = rf_clf.predict(X_test)
accuracy = accuracy_score(y_test.astype(int), pred)
print(accuracy)
```

</details>

<details>
<summary>코드 셀 8 보기</summary>

```python
rf_clf1 = RandomForestClassifier(n_estimators=300, max_depth=12, min_samples_leaf=8, \
                                 min_samples_split=8, random_state=42)
rf_clf1.fit(X_train , y_train.astype(int))
pred = rf_clf1.predict(X_test)
print(accuracy_score(y_test.astype(int) , pred))
```

</details>

<details>
<summary>코드 셀 10 보기</summary>

```python
from sklearn.ensemble import GradientBoostingClassifier
import time

X_train, X_test, y_train, y_test = get_human_dataset()

start_time = time.time()

gb_clf = GradientBoostingClassifier(random_state=42)
gb_clf.fit(X_train , y_train.astype(int))
gb_pred = gb_clf.predict(X_test)
gb_accuracy = accuracy_score(y_test.astype(int), gb_pred)

print(gb_accuracy)
print(time.time() - start_time)
```

</details>

## 결과 메모

앙상블의 효과는 모델 수보다 오류 패턴의 다양성에서 나온다. 비슷한 모델만 결합하면 계산량에 비해 개선이 작을 수 있다.

## 사용한 함수

| 함수·클래스 | 역할 |
|---|---|
| `load_breast_cancer` | 유방암 분류 데이터셋 불러오기 |
| `read_csv` | CSV 파일을 DataFrame으로 읽기 |
| `train_test_split` | 학습용과 평가용 데이터 분리 |
| `GridSearchCV` | 파라미터 조합을 교차검증으로 탐색 |
| `LogisticRegression` | 확률 기반 선형 분류 모델 |
| `RandomForestClassifier` | 여러 무작위 트리를 결합한 분류 모델 |
| `GradientBoostingClassifier` | 오류를 순차 보정하는 부스팅 분류 모델 |
| `accuracy_score` | 분류 정확도 계산 |

## 정리

- 모든 기반 모델을 같은 검증 데이터로 비교한다.
- Soft Voting에서는 확률 품질을 확인한다.
- Boosting은 학습률과 트리 수를 함께 조정한다.
