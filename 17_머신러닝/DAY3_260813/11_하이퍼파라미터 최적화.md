# 11. 하이퍼파라미터 최적화

Hyperopt의 탐색공간과 목적함수를 이해하고 XGBoost 파라미터 최적화에 적용한다.

[← DAY3 목차](<README.md>) · [원본 노트북](<11_하이퍼파라미터 최적화.ipynb>)

## 실습 내용

- hp 모듈로 탐색공간을 정의한다.
- 목적함수가 최소화되는 구조를 이해한다.
- Trials에 후보와 손실을 누적한다.
- 교차검증 AUC를 이용해 XGBoost를 튜닝한다.

**데이터**  
수학 목적함수 예제와 유방암 XGBoost 분류

## 핵심 메모

### 탐색공간

정수·연속·범주형 파라미터의 가능한 범위를 정의한다.

### 목적함수

파라미터를 받아 모델을 검증하고 최소화할 loss를 반환한다.

### TPE

이전 탐색 결과를 이용해 유망한 영역의 후보를 더 자주 선택한다.

## 코드

코드는 길어서 중요한 셀만 접어 두었다. 전체 실행 과정은 원본 노트북에서 확인한다.

<details>
<summary>코드 셀 9 보기</summary>

```python
import pandas as pd
import numpy as np
from sklearn.datasets import load_breast_cancer
from sklearn.model_selection import train_test_split
import warnings
warnings.filterwarnings('ignore')

dataset = load_breast_cancer()

cancer_df = pd.DataFrame(data=dataset.data, columns=dataset.feature_names)
cancer_df['target']= dataset.target
X_features = cancer_df.iloc[:, :-1]
y_label = cancer_df.iloc[:, -1]
```

</details>

<details>
<summary>코드 셀 10 보기</summary>

```python
X_train, X_test, y_train, y_test=train_test_split(X_features, y_label,
                                                test_size=0.2, random_state=156 )

X_tr, X_val, y_tr, y_val= train_test_split(X_train, y_train, 
                                           test_size=0.1, random_state=156 )
```

</details>

<details>
<summary>코드 셀 14 보기</summary>

```python
from sklearn.metrics import confusion_matrix, accuracy_score
from sklearn.metrics import precision_score, recall_score
from sklearn.metrics import f1_score, roc_auc_score

def get_clf_eval(y_test, pred=None, pred_proba=None):
    confusion = confusion_matrix( y_test, pred)
    accuracy = accuracy_score(y_test , pred)
    precision = precision_score(y_test , pred)
    recall = recall_score(y_test , pred)
    f1 = f1_score(y_test,pred)

    roc_auc = roc_auc_score(y_test, pred_proba)
    print('오차 행렬')
    print(confusion)

    print('정확도: {0:.4f}, 정밀도: {1:.4f}, 재현율: {2:.4f},\
    F1: {3:.4f}, AUC:{4:.4f}'.format(accuracy, precision, recall, f1, roc_auc))
```

</details>

<details>
<summary>코드 셀 15 보기</summary>

```python
xgb_wrapper = XGBClassifier(n_estimators=400,
                            learning_rate=round(best['learning_rate'], 5),
                            max_depth=int(best['max_depth']),
                            min_child_weight=int(best['min_child_weight']),
                            colsample_bytree=round(best['colsample_bytree'], 5),
                            early_stopping_rounds=50, eval_metric='logloss'
                           )

evals = [(X_tr, y_tr), (X_val, y_val)]
xgb_wrapper.fit(X_tr, y_tr, eval_set=evals, verbose=True)

preds = xgb_wrapper.predict(X_test)
pred_proba = xgb_wrapper.predict_proba(X_test)[:, 1]

get_clf_eval(y_test, preds, pred_proba)
```

</details>

## 실습 흐름

조정할 파라미터의 범위와 평가 기준을 먼저 정한 뒤 여러 조합을 교차검증으로 비교했다. 탐색 과정에서 가장 좋은 조합과 점수를 확인하고, 선택된 설정으로 모델을 다시 학습해 별도 데이터에서 평가했다. 탐색 횟수를 늘리는 것보다 올바른 검증 구조와 범위를 정하는 일이 먼저라는 점을 확인했다.

## 결과 메모

최고 파라미터는 탐색에 사용한 검증 점수에 맞춰져 있다. 별도 테스트 평가와 반복 교차검증으로 개선이 안정적인지 확인해야 한다.

### 실행 결과

```text
100%|██████████| 5/5 [00:00<00:00, 765.33trial/s, best loss: -224.0]
best: {'x': np.float64(-4.0), 'y': np.float64(12.0)}
100%|██████████| 20/20 [00:00<00:00, 1665.93trial/s, best loss: -296.0]
best: {'x': np.float64(2.0), 'y': np.float64(15.0)}
100%|██████████| 50/50 [00:05<00:00,  9.50trial/s, best loss: -0.964868711513884]
오차 행렬
정확도: 0.9649, 정밀도: 0.9740, 재현율: 0.9740,    F1: 0.9740, AUC:0.9923
```

## 사용한 함수

| 함수·클래스 | 역할 |
|---|---|
| `load_breast_cancer` | 유방암 분류 데이터셋 불러오기 |
| `train_test_split` | 학습용과 평가용 데이터 분리 |
| `cross_val_score` | 교차검증 점수 계산 |
| `XGBClassifier` | XGBoost 분류 모델 |
| `accuracy_score` | 분류 정확도 계산 |
| `confusion_matrix` | TN·FP·FN·TP 혼동행렬 계산 |
| `precision_score` | 정밀도 계산 |
| `recall_score` | 재현율 계산 |
| `f1_score` | 정밀도와 재현율의 조화평균 계산 |
| `roc_auc_score` | 확률 순위 기반 ROC-AUC 계산 |

## 다시 볼 것

- 테스트 데이터는 최종 조합을 선택하는 탐색 과정에 넣지 않는다.
- 파라미터 범위가 지나치게 넓거나 촘촘하면 시간만 크게 늘 수 있다.
- 최적 점수뿐 아니라 비슷한 성능을 내는 더 단순한 설정도 확인한다.

## 정리

- 테스트 점수를 목적함수로 사용하지 않는다.
- 정수형 파라미터는 모델 전달 전에 형변환한다.
- 탐색 범위와 예산을 먼저 합리적으로 제한한다.
