# 15. 회귀 분석

선형회귀, 다항회귀, Ridge와 Lasso를 통해 연속값 예측과 규제의 효과를 이해한다.

[← DAY5 목차](<README.md>) · [원본 노트북](<15_회귀 분석.ipynb>)

## 실습 내용

- 잔차와 MSE/RMSE를 이해한다.
- 다항 차수에 따른 과소·과대적합을 비교한다.
- Ridge/Lasso의 규제 강도를 교차검증한다.
- 타깃과 피처의 왜도를 변환한다.

**데이터**  
합성 회귀 데이터와 주택 관련 회귀 데이터

## 핵심 메모

### 잔차

실제값과 예측값의 차이로 모델이 놓친 구조를 보여준다.

### RMSE

큰 오차에 더 큰 벌점을 주며 목표변수와 같은 단위로 해석한다.

### Ridge/Lasso

계수를 제한해 복잡도를 낮추며 Lasso는 일부 계수를 0으로 만들 수 있다.

## 코드

코드는 길어서 중요한 셀만 접어 두었다. 전체 실행 과정은 원본 노트북에서 확인한다.

<details>
<summary>코드 셀 3 보기</summary>

```python
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_squared_error, r2_score

y_target= df['PRICE']
X_data=df.drop(['PRICE'], axis=1, inplace=False)

X_train, X_test, y_train, y_test=train_test_split(X_data, y_target, test_size=0.3, random_state=42)

lr=LinearRegression()
lr.fit(X_train, y_train)
pred=lr.predict(X_test)
print(pred)

mse=mean_squared_error(y_test, pred)
rmse=np.sqrt(mse)

print(mse)
print(rmse)
print(r2_score(y_test,pred))
```

</details>

<details>
<summary>코드 셀 13 보기</summary>

```python
plt.figure(figsize=(14, 5))
degrees = [1, 4, 15]

for i in range(len(degrees)):
    ax = plt.subplot(1, len(degrees), i + 1)
    plt.setp(ax, xticks=(), yticks=())
    polynomial_features = PolynomialFeatures(degree=degrees[i],include_bias=False)
    linear_regression = LinearRegression()
    pipeline = Pipeline([('polynomial_features', polynomial_features), ('linear_regression', linear_regression)])

    pipeline.fit(X.reshape(-1, 1), y)

    scores = cross_val_score(pipeline,X.reshape(-1, 1), y,scoring='neg_mean_squared_error',cv=10)
    coefficients = pipeline.named_steps['linear_regression'].coef_
    print('Degree', degrees[i])
    print('회귀 계수', np.round(coefficients, 2))
    print('MSE', -np.mean(scores))
    X_test = np.linspace(0, 1, 100)

    plt.plot( X_test,pipeline.predict(X_test[:, np.newaxis]))

    plt.plot(X_test,true_fun(X_test),'--',label='True function')

    plt.scatter(X, y,edgecolor='b',s=20,label='Samples')

    plt.xlabel('X')
    plt.ylabel('y')
    plt.xlim((0, 1))
    plt.ylim((-2, 2))
    plt.legend(loc='best')
    plt.title( f'Degree {degrees[i]} MSE: {-scores.mean():.2f}')

# ... 이하 코드는 원본 노트북 참조
```

</details>

<details>
<summary>코드 셀 16 보기</summary>

```python
fig, axs = plt.subplots(figsize = (18, 6), nrows = 1, ncols = 5)

coeff_df = pd.DataFrame()

for pos, alpha in enumerate(alphas):
    ridge = Ridge(alpha = alpha)
    ridge.fit(X_data, y_target)
    coeff=pd.Series(data=ridge.coef_,index=X_data.columns)
    colname='alpha:'+str(alpha)
    coeff_df[colname]=coeff
    coeff=coeff.sort_values(ascending=False)
    axs[pos].set_title(colname)
    axs[pos].set_xlim(-1,1)
    sns.barplot(x = coeff.values, y =coeff.index, ax = axs[pos])
plt.tight_layout()
plt.show()
```

</details>

<details>
<summary>코드 셀 18 보기</summary>

```python
from sklearn.linear_model import Lasso, ElasticNet

def get_linear_reg_eval(model_name,
                         params=None,
                         X_data_n=None,
                         y_target_n=None,
                         verbose=True,
                         return_coeff=True):
    coeff_df = pd.DataFrame()
    if verbose: print("#####", model_name, "#####")
    for param in params:
        if model_name == "Ridge": model = Ridge(alpha=param)
        elif model_name == "Lasso": model = Lasso(alpha=param)
        elif model_name == "ElasticNet":
            model = ElasticNet(alpha=param, l1_ratio=0.7)

        neg_mse_scores = cross_val_score(model,
                                         X_data_n,
                                         y_target_n,
                                         scoring="neg_mean_squared_error",
                                         cv=5)
        avg_rmse = np.mean(np.sqrt(-1 * neg_mse_scores))
        print("alpha {0} rmse {1}".format(param, avg_rmse))

        model.fit(X_data_n, y_target_n)
        if return_coeff:
            coeff = pd.Series(data=model.coef_,
                              index=X_data_n.columns)
            colname = "alpha: " + str(param)
            coeff_df[colname] = coeff

    return coeff_df
```

</details>

## 실습 흐름

합성 데이터로 선형회귀의 계수와 잔차를 확인한 뒤 다항식 차수를 높여 예측 곡선이 어떻게 변하는지 비교했다. 이어서 Ridge와 Lasso의 규제 강도를 여러 값으로 교차검증하고 RMSE가 가장 낮은 구간을 찾았다. 분포가 한쪽으로 긴 피처와 목표값에는 로그 변환을 적용해 결과 변화를 살펴봤다.

## 결과 메모

교차검증 RMSE의 평균과 fold별 편차를 함께 본다. 규제 강도 alpha는 피처 스케일과 함께 해석해야 한다.

### 실행 결과

```text
MSE 0.4077289625098685
MSE 0.04320874987232056
MSE 0.21959838159883258
Negative MSE [-0.484 -0.623 -0.646 -0.544 -0.494]
RMSE [0.695 0.789 0.804 0.737 0.703]
Average RMSE 0.745820729297258
alpha 0, RMSE 0.746
alpha 0.1, RMSE 0.746
alpha 1, RMSE 0.746
alpha 10, RMSE 0.746
```

## 사용한 함수

| 함수·클래스 | 역할 |
|---|---|
| `fetch_california_housing` | California Housing 회귀 데이터 불러오기 |
| `train_test_split` | 학습용과 평가용 데이터 분리 |
| `cross_val_score` | 교차검증 점수 계산 |
| `StandardScaler` | 평균 0, 표준편차 1로 표준화 |
| `LinearRegression` | 일반 선형회귀 모델 |
| `Ridge` | L2 규제 선형회귀 모델 |
| `Lasso` | L1 규제 선형회귀 모델 |
| `mean_squared_error` | 평균제곱오차 계산 |
| `r2_score` | 평균 기준선 대비 설명력 계산 |

## 다시 볼 것

- 훈련 RMSE와 검증 RMSE가 함께 낮아지는지 확인한다.
- 다항 차수를 높일수록 훈련 오차는 줄어도 검증 오차는 커질 수 있다.
- 규제 전에는 피처 스케일 차이가 계수에 미치는 영향을 확인한다.

## 정리

- 규제 회귀 전에 수치 피처를 스케일링한다.
- 훈련 점수만으로 다항 차수를 선택하지 않는다.
- R2와 실제 오차 단위를 함께 확인한다.
